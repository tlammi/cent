TESTS += {
  'srcs': files('task.cpp'),
  'deps': libcent_dep
}

TESTS += {
  'srcs': files('yield.cpp'),
  'deps': libcent_dep
}

TESTS += {
  'srcs': files('sleep.cpp'),
  'deps': libcent_dep,
}

TESTS += {
  'srcs': files('synchronization.cpp'),
  'deps': libcent_dep,
}
