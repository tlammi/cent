TESTS += {
  'srcs': files('task.cpp'),
  'deps': libcent_dep
}

TESTS += {
  'srcs': files('yield.cpp'),
  'deps': libcent_dep
}

# TESTS += {
#   'srcs': files('executor.cpp'),
#   'deps': libcent_dep
# }
