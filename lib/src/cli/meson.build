
srcs = files('parser.cpp')

libcent_cli = library('cent-cli', srcs, include_directories: libcent_incdir)
libcent_cli_dep = declare_dependency(include_directories: libcent_incdir, link_with: libcent_cli)
